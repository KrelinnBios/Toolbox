# AGENTS.md

本文件适用于整个仓库，作为 AI 编码代理修改本项目时的项目规范。除非用户另有要求，使用中文回复和说明。

## 项目概览

Toolbox 是一个纯静态的工具导航网页，用于集中管理和快速访问常用在线工具。项目不使用任何构建工具或前端框架，只包含原生 HTML、CSS 和 JavaScript。

**技术栈：**
- 原生 HTML5
- 原生 CSS3（包含自定义滚动条样式）
- 原生 JavaScript（ES6+）
- 部署平台：Cloudflare Pages

**文件结构：**
- `index.html` - 网页主体结构和元数据
- `toolbox.css` - 全部样式，包含响应式布局和滚动条样式
- `toolbox.js` - 搜索、目录定位、排序和渲染逻辑
- `sites.js` - 站点数据和分类定义
- `favicon.svg` - 网站图标
- `screenshots/` - README 使用的界面截图

## 开始任务前

修改前应先检查：

1. **浏览器兼容性**：确认新增特性在主流浏览器中的支持情况
2. **静态资源版本号**：HTML 中的 CSS/JS 引用带有版本参数（`?v=YYYYMMDDX`）
3. **滚动条样式**：项目已实现符合 RepoRules WEB.md 标准的自定义滚动条
4. **无障碍性**：保持现有的 ARIA 标签和语义化 HTML 结构

## 修改原则

- **保持纯静态**：不引入构建工具、前端框架或 npm 依赖
- **原生优先**：使用原生 JavaScript，不使用 jQuery 或其他库
- **最小修改**：只改必要的代码，不重构无关部分
- **响应式保持**：修改样式时确保桌面和移动端都正常显示
- **性能优先**：避免不必要的 DOM 操作和重排

## 关键约束

### 静态资源版本控制

所有静态资源（CSS、JS、favicon）在 HTML 中引用时必须带版本参数：

```html
<link rel="stylesheet" href="./toolbox.css?v=202609091">
<script src="./sites.js?v=202609091" defer></script>
<script src="./toolbox.js?v=202609091" defer></script>
```

修改 CSS 或 JS 文件后，必须更新所有相关版本号：

- 格式：`YYYYMMDDX`（年月日 + 当日序号）
- 同一天的多次修改递增序号（1, 2, 3...）
- 确保 HTML 中所有对该文件的引用都更新版本号

### 滚动条样式

项目已实现符合 RepoRules WEB.md 规范的自定义滚动条（`toolbox.css` 第 1-67 行），包括：

- 6px 宽度
- 透明轨道
- 999px 圆角（胶囊形）
- 隐藏箭头按钮
- hover 和 active 使用同一主题色

**不要删除或修改滚动条相关样式**，除非用户明确要求。

### 站点数据结构

`sites.js` 中的站点数据必须遵循以下结构：

```javascript
const siteCategories = [
  {
    id: "category-id",        // 唯一标识符，kebab-case
    label: "分类名称",         // 显示给用户的中文名称
    items: [
      {
        name: "站点名称",      // 简短的站点名称
        url: "https://...",   // 完整 URL，必须包含协议
        desc: "站点描述",      // 用于搜索和帮助用户识别用途
        group: "用途小组"      // 同一分类内的内部归组字段，不直接显示
      }
    ]
  }
];
```

同一大分类内，定位相近的站点使用 `group` 字段归组。页面不显示小组标题，只让同组站点连续排列，并按中文拼音或英文名称排序。

### 目录与滚动定位

- 桌面端左侧目录固定在视口左侧，移动端使用横向目录栏
- 点击分类目录时滚动到右侧对应分类，不隐藏其他分类
- 右侧滚动时，以视口高度约 `2/5` 的位置判断当前分类并高亮左侧目录
- 滚动到页面底部时，高亮最后一个分类
- 搜索时保留完整目录，目录点击会清空搜索并恢复完整内容

### 无障碍要求

- 保持现有的语义化 HTML 结构（`<aside>`, `<main>`, `<section>`）
- 保留 ARIA 标签（`aria-label`, `aria-labelledby`, `role`, `aria-live`）
- 搜索框必须有 `<label>` 关联
- 交互元素必须可键盘访问
- 隐藏内容使用 `hidden` 属性，而非 `display: none`

### 搜索和排序

搜索逻辑在 `toolbox.js` 中实现：

- 实时搜索站点名称和描述
- 大小写不敏感
- 支持站点名称、描述、域名和分类名称匹配
- 搜索无结果时显示提示
- 实时更新显示的站点数量
- 同一分类内先按 `group` 分组，再按中文拼音或英文名称排序

## 已确立的决定

### 为什么不使用构建工具？

项目足够简单，使用构建工具会增加复杂度而无明显收益。直接编辑源文件后推送到 GitHub，Cloudflare Pages 自动部署，流程简洁高效。

### 为什么不使用前端框架？

项目只需要简单的搜索、筛选和渲染，原生 JavaScript 完全够用。引入 React/Vue 会增加体积和加载时间，降低性能。

### 为什么使用 `defer` 而不是 `async`？

`defer` 保证脚本按顺序执行，`sites.js` 必须在 `toolbox.js` 之前加载。`async` 会导致执行顺序不确定。

### 为什么站点描述有些是英文？

部分站点本身就是英文网站，保留原始描述有助于用户识别。描述主要用于搜索匹配，而非完整翻译。

## 验证

修改后至少检查：

1. **浏览器测试**：在 Chrome/Edge、Firefox、Safari 中打开 `index.html`
2. **搜索功能**：输入关键词确认能正确筛选
3. **目录定位**：点击分类标签确认页面滚动到对应分类
4. **滚动联动**：滚动到分类中部和页面底部，确认左侧高亮跟随
5. **响应式布局**：调整浏览器窗口大小，确认移动端正常显示
6. **滚动条样式**：确认滚动条显示为 6px 宽度、透明轨道、胶囊形
7. **无障碍**：使用 Tab 键确认可键盘导航
8. **版本号**：确认 HTML 中的版本参数已更新

修改样式后检查桌面端（> 800px）和移动端（≤ 800px）的媒体查询是否正常。

## 文档同步

- 添加新站点分类时，更新 README.md "功能概览"章节
- 修改项目结构时，更新 README.md "项目结构"章节
- 添加新功能时，更新 README.md "功能概览"和"使用方式"章节

## 提交约定

- 提交消息使用中文，简明扼要
- 一次提交专注一个主题
- 添加站点时说明数量和类别（如"添加 3 个图像处理工具"）
- 修改样式时说明改动内容（如"优化移动端布局"）
- 修复问题时说明问题和修复方式（如"修复搜索框在 Safari 中的样式问题"）
